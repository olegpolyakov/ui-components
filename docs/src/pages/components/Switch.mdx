import Demo from '@/components/Demo';
import Section from '@/components/Section';

import Switch from '~/components/Switch';

# Switch

Switch is a component that allows users to toggle between two states, such as on and off.

<Demo settings={Switch.settings}>
    <Switch />
</Demo>