import Demo from '@/components/Demo';
import Section from '@/components/Section';

import Slider from '~/components/Slider';

# Slider

Slider is a component that allows users to select a value or range of values by dragging a handle along a track.

<Demo settings={Slider.settings}>
  <Slider />
</Demo>