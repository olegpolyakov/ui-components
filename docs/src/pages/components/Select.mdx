import Demo from '@/components/Demo';
import Section from '@/components/Section';

import Flex from '~/components/Flex';
import Select, { Option } from '~/components/Select';

# Select

Select is a component that allows users to select an option from a dropdown list or enter a custom value.

<Demo settings={Select.settings}>
  <Select label="Label" defaultValue={[]} placeholder="Placeholder">
    <Option value="1" content="Item 1" />
    <Option value="2" content="Item 2" />
    <Option value="3" content="Item 3" />
  </Select>
</Demo>
