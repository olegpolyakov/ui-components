import Demo from '@/components/Demo';
import Section from '@/components/Section';

import Card from '~/components/Card';

# Card

Card is a component that displays content and actions on a single topic, often used to group related information.

<Demo settings={Card.settings}>
    <Card
        content="This is a simple card component."
        variant="outlined"
    />
</Demo>