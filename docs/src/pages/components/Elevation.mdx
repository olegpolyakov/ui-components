import Demo from '@/components/Demo';
import Section from '@/components/Section';

import Button from '~/components/Button';
import Drawer from '~/components/Drawer';

# Elevation

Elevation is a component that adds depth to elements by applying shadow effects, enhancing the visual hierarchy of the interface.

<Demo settings={Elevation.settings}>
    <Elevation z={z} />
</Demo>