import Demo from '@/components/Demo';
import Section from '@/components/Section';

import { ProgressBar } from '~/components/Progress';

# Progress

Progress is a component that visually represents the completion status of a task or process.

<Demo settings={ProgressBar.settings}>
    <ProgressBar value={42} />
</Demo>