import Demo from '@/components/Demo';
import Section from '@/components/Section';

import Divider from '~/components/Divider';

# Divider

Divider is a component that visually separates content into distinct sections, enhancing readability and organization.

<Demo
    settings={Divider.settings}
    wrap={<div style={{display: 'flex', width: '100%', height: '200px'}} />}
>
    <Divider />
</Demo>