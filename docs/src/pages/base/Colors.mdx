import {
  baseColor,
  brandColor,
  accentColor,
  dangerColor,
  infoColor,
  successColor,
  warningColor
} from '~/styles/vars.module.scss';

import { Flex } from "~/components";

import Box from "@/components/Box";
import Color from "@/components/Color";
import Palette from "@/components/Palette";

# Colors

<Flex gap="xxs" wrap>
    <Color name="Base" value={baseColor} />
    <Color name="Brand" value={brandColor} />
    <Color name="Accent" value={accentColor} />
    <Color name="Danger" value={dangerColor} />
    <Color name="Info" value={infoColor} />
    <Color name="Success" value={successColor} />
    <Color name="Warning" value={warningColor} />
</Flex>

## Palettes

<Flex column gap="m">
  <Palette name="base" alpha={false} />
  <Palette name="brand" alpha={false} />
  <Palette name="accent" alpha={false} />
  <Palette name="success" alpha={false} />
  <Palette name="warning" alpha={false} />
  <Palette name="danger" alpha={false} />
  <Palette name="info" alpha={false} />
</Flex>

## Borders

<Flex gap="xxs">
  <Flex column gap="xxs">
    <Box style={{borderWidth: '1px', borderStyle: 'solid'}} className="ui-border-color">Base/Primary</Box>
    <Box style={{borderWidth: '1px', borderStyle: 'solid'}} className="ui-border-secondary-color">Secondary</Box>
    <Box style={{borderWidth: '1px', borderStyle: 'solid'}} className="ui-border-tertiary-color">Tertiary</Box>
    <Box style={{borderWidth: '1px', borderStyle: 'solid'}} className="ui-border-inverted-color">Inverted</Box>
  </Flex>

  {['brand', 'accent', 'success', 'warning', 'danger', 'info'].map(name => (
    <Flex column gap="xxs" key={name}>
      <Box style={{borderWidth: '1px', borderStyle: 'solid'}} className={`ui-${name}-border-color`}>Base/Primary</Box>
      <Box style={{borderWidth: '1px', borderStyle: 'solid'}} className={`ui-${name}-border-secondary-color`}>Secondary</Box>
      <Box style={{borderWidth: '1px', borderStyle: 'solid'}} className={`ui-${name}-border-tertiary-color`}>Tertiary</Box>
      <Box style={{borderWidth: '1px', borderStyle: 'solid'}} className={`ui-${name}-border-inverted-color`}>Inverted</Box>
    </Flex>
  ))}
</Flex>

## Backgrounds

<Flex gap="xxs">
  <Flex column gap="xxs">
    <Box className="ui-background-color">Base/Primary</Box>
    <Box className="ui-background-secondary-color">Secondary</Box>
    <Box className="ui-background-tertiary-color">Tertiary</Box>
    <Box className="ui-background-inverted-color ui-foreground-inverted-color">Inverted</Box>
  </Flex>

  {['brand', 'accent', 'success', 'warning', 'danger', 'info'].map(name => (
    <Flex column gap="xxs" key={name}>
      <Box className={`ui-${name}-background-color`}>Base/Primary</Box>
      <Box className={`ui-${name}-background-secondary-color`}>Secondary</Box>
      <Box className={`ui-${name}-background-tertiary-color`}>Tertiary</Box>
      <Box className={`ui-${name}-background-inverted-color ui-foreground-inverted-color`}>Inverted</Box>
    </Flex>
  ))}
</Flex>

## Foreground

<Flex gap="xs">
  <Flex column gap="xxs">
    <span className="ui-foreground-color">Base/Primary</span>
    <span className="ui-foreground-secondary-color">Secondary</span>
    <span className="ui-foreground-tertiary-color">Tertiary</span>
    <span className="ui-foreground-tertiary-inverted-color">Inverted</span>
  </Flex>

  {['brand', 'accent', 'success', 'warning', 'danger', 'info'].map(name => (
    <Flex column gap="xxs" key={name}>
      <span className={`ui-${name}-foreground-color`}>Base/Primary</span>
      <span className={`ui-${name}-foreground-secondary-color`}>Secondary</span>
      <span className={`ui-${name}-foreground-tertiary-color`}>Tertiary</span>
      <span className={`ui-${name}-foreground-tertiary-inverted-color`}>Inverted</span>
    </Flex>
  ))}
</Flex>