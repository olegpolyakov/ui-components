import Demo from '@/components/Demo';
import Section from '@/components/Section';

import Button from '~/components/Button';
import Popover from '~/components/Popover';

# Popover

Popover is a component that displays a floating content box anchored to a trigger element, often used for tooltips or additional information.

<Demo settings={Popover.settings}>
    <Popover
        trigger={<Button>Trigger</Button>}
        content="Content"
        variant="filled"
    />
</Demo>