import Demo from '@/components/Demo';
import Section from '@/components/Section';

import Tooltip from '~/components/Tooltip';

# Tooltip

Tooltip is a component that provides additional information when users hover over or focus on an element.

<Demo settings={Tooltip.settings}>
    <Tooltip content="Tooltip content" placement='top'>
        <span>Hover me</span>
    </Tooltip>
</Demo>
