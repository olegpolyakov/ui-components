import Demo from '@/components/Demo';
import Section from '@/components/Section';

import Button from '~/components/Button';

# Button

Button is a component that allows users to perform actions with a single click or tap.

<Demo settings={Button.settings}>
  <Button
    content="Button"
    onClick={() => alert('Button clicked!')}
  />
</Demo>